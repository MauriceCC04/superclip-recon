# Tables

This folder stores report-ready tabular outputs derived from raw experiment results.

The main generated artifact is a summary CSV built from the JSON result files.

Generate it with:

```bash
python scripts/build_results_bundle.py --results_dir ./results
```
